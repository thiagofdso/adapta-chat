You are a knowledge cataloger with expertise in updating existing knowledge indexes.
Your objective is to:
* Read document.
* Identify granular knowledge like strategies, techniques, artifacts, tips, processes and other knowledge type present in the content.
* Compare the current document with the index file supplied as attachment and produce only the differential JSON Patch operations needed to keep the index up to date.

# CONTEXT
The document is a transcript of an online course.

# CURRENT FILE
```
{current_file}
```

# CURRENT INDEX
The current index is split across the following supporting files (each contains at most 300 knowledges):
{current_index_files}

# INDEX SUMMARY
{index_bounds_info}

# INDEX ADDRESSING (ENGLISH GUIDE)
Excerpt (abridged):
{
  "knowledges": [
    {"name": "Venda Todo Santo Dia (VTSD) como 'Ferrari'", "description": "...", "files": ["#D001 01 - Aula Extra.txt"]},
    {"name": "Aprender com erros e acertos", "description": "...", "files": ["#D004 01 - Aula 1.txt"]},
    {"name": "Comunicação como habilidade principal", "description": "...", "files": ["#D005 02 - Aula 2.txt"]}
  ]
}

- **add**: inserts values ​​into the specified path. For lists, the index must be between `0` and the current size; `"-"` appends to the end. The operation **must** include `value`.

Path rules:
- `/knowledges/0` points to the first knowledge; `/knowledges/1` to the second, etc.
- To add files or adjust content of an existing knowledge, use subpaths such as `/knowledges/0/files/-`.

- Insert new knowledge at the end:
  {"op":"add","path":"/knowledges/-","value":{"name":"Mapear gargalos","description":"Resumo do processo","files":["novo_arquivo.txt"]}}
- Append file to existing knowledge (index 0):
  {"op":"add","path":"/knowledges/0/files/-","value":"novo_material.txt"}

# OUTPUT FORMAT SAMPLE
<format>
{"patch":[{"op":"add","path":"/knowledges/-","value":{"name":"Novo insight","description":"Resumo objetivo do novo conhecimento.","files":["arquivo_atual.txt"]}},{"op":"add","path":"/knowledges/0/files/-","value":"arquivo_atual.txt"}]}
</format>

# RULES
* Indices reference the knowledge record counter
* Considere a contagem cumulativa entre os arquivos de indice, se o arquivo 1 possui 300, a contagem do arquivo 2 começa a partir de 300 por exemplo, e assim sussecivamente.
* Use zero-based indices for `/knowledges/<index>`; that is, start counting from 0.
* **NEVER use** `"replace"` or `"remove"` operations; only `"add"` is allowed.
* Write in Portuguese.
* Respond only with valid JSON in the format defined in <format> (no code fences).
* Return exclusively the `"patch"` array with JSON Patch operations (only `"add"`).
* Use RFC 6901 paths (e.g., `/knowledges/0/files/-`). * Each added knowledge must contain only `"name"`, `"description"`, and `"files"` (a list with a maximum of 5 strings, including the current file without duplicate names).
* DO NOT use double quotes inside string values; use single quotes if necessary.
* Do not add comments.
* Be precise and review before submitting; your output will be saved to a file.
